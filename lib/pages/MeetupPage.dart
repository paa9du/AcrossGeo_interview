import 'package:flutter/material.dart';

class MeetupTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Meetup Tab Content'),
    );
  }
}
